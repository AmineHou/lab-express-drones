<h2>Available drones</h2>
{{#each drones}}
  <ul>
    <li>{{this.name}}</li>
    <li> {{this.propellers}}</li>
    <li>{{this.maxSpeed}}</li>
  </ul>
  <a href="/drones/{{this._id}}/edit">Edit</a>
  <form action="/drones/{{this._id}}/delete" method="post">
    <button>Delete</button>
  </form>
{{/each}}